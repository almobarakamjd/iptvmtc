# Todo — تطبيق IPTV لأندرويد (MTC)

## الخلفية (2026-07-26)

- وضع المطوّر في سامسونج TU8000 ثبت عطبه نهائياً (sdbd يرفض المصافحة مهما كانت الشبكة — جُرّب كل شيء حتى ضبط المصنع)
- القرار: التحول لأجهزة أندرويد (تلفزيون MTC أو Android Box) حيث التثبيت من فلاش USB مباشرة

## المهام

- [x] إنشاء مشروع الغلاف الأندرويدي ونسخ تطبيق الويب إلى assets
- [x] بناء أول APK بنجاح (2026-07-26) — الملف الجاهز: `SmartTVPlus.apk` في جذر المشروع (~190KB)
- [x] جهاز الاختبار الفعلي: بوكس MTC، أندرويد 9.0، رام 1GB فقط، تخزين 8GB — تم شراؤه وتجربة APK عليه (2026-07-26)
- [x] حل مشكلة الشاشة الزرقاء عند تشغيل أي بث: WebView على أندرويد لا يشغّل m3u8 (HLS) مباشرة كسفاري — أُضيفت مكتبة hls.js محلياً (2026-07-26)
- [x] hls.js لم يكفِ: الصوت اشتغل لكن الصورة بقيت سوداء (WebView على هذا البوكس لا يعرض فيديو MediaSource) — استُبدل بمحرك ExoPlayer/media3 حقيقي مضمَّن خلف WebView شفاف (2026-07-26)
- [x] إعدادات لاختيار محرك التشغيل (ExoPlayer/WebView) بشكل مستقل للمباشر وللأفلام/المسلسلات (2026-07-26)
- [x] ExoPlayer جمّد التطبيق كاملاً على رام 1GB (حتى الإعدادات) — تقرر التخلي عن أي محرك فيديو مُضمَّن نهائياً، والتشغيل الآن عبر نية `ACTION_VIEW` تفتح VLC/MX Player المثبَّتين أصلاً على الجهاز (2026-07-27)
- [ ] تجربة التشغيل الفعلي عبر VLC/MX على بوكس MTC — لم تُختبر على الجهاز الحقيقي بعد، فقط تأكدنا أن البناء ينجح وحجم APK رجع لطبيعي (~390KB)
- [ ] ميزة الإعادة (catch-up/timeshift) والترجمة والتسجيل غير متاحة الآن على أندرويد بعد التحول لمشغّل خارجي — تحتاج نقاش منفصل لو صارت مطلوبة
- [ ] جسر أندرويد لقراءة ترجمات SRT من فلاش USB (بديل tizen.filesystem) — ملاحظة: أصبح أقل أهمية بعد التحول لمشغّل خارجي
- [ ] ربط التطبيق بهوية الجهاز (Android ID) بحيث لا يعمل إلا على جهاز أبو فيصل — كان مطلباً سابقاً
- [ ] توقيع release بمفتاح خاص بدل debug (اختياري — للثبات طويل الأمد)
- [ ] مراجعة لوحة المفاتيح الافتراضية الجديدة (عربي/إنجليزي) على الجهاز الفعلي — لم تُختبر إلا بالبناء فقط
- [x] نسخ احتياطي سحابي مشفّر (اشتراكات + مفضلة + مشاهدات أخيرة) عبر kvdb.io + AES-GCM، مع رمز جهاز للاستيراد على جهاز آخر (2026-07-27)
- [ ] تجربة النسخ الاحتياطي/الاستيراد فعلياً على الجهاز — لم تُختبر إلا بالبناء فقط
- [ ] غير ممكن حالياً: حفظ "مكان التوقف" داخل كل فيلم/مسلسل ضمن النسخة الاحتياطية — التشغيل خارجي (VLC/MX) ولا يُطلعنا على تقدّم المشاهدة؛ يحتاج نقاش/حل منفصل لو صار مطلوباً فعلاً
- [x] النسخ الاحتياطي كان يفشل فعلياً: kvdb.io صار يشترط حقل email بجسم الطلب عند إنشاء أول مكان تخزين (بلا تسجيل حقيقي) ولم نكن نرسله — تأكدنا بالتجربة المباشرة عبر curl وصُحِّح بسطر واحد في cloud.js (2026-07-27). لا حاجة لمستودع GitHub بديل.
- [x] إعداد "المشغل المفضل" (VLC/MX/اسأل دائماً) بدل نافذة الاختيار كل مرة (2026-07-27)
- [x] قوائم مخصّصة للقنوات المباشرة فقط (إنشاء/تسمية/إضافة/إزالة) منفصلة عن المفضلة (2026-07-27)
- [x] تبديل القناة بزر التالي/السابق بالريموت أثناء العرض: عبر تسليم VLC/MX قائمة تشغيل M3U لقنوات نفس التصنيف/القائمة بدل رابط قناة واحدة — بموفّر ملفات صغير مكتوب يدوياً (`PlaylistProvider.java`، بلا أي مكتبة خارجية) (2026-07-27)
- [x] رمز النسخة الاحتياطية يبقى حتى بعد حذف التطبيق وإعادة تثبيته — يُحفظ بملف على ذاكرة الجهاز المشتركة (خارج مجلد التطبيق) عبر جسر `AndroidStorage` جديد (2026-07-27)
- [x] لغة الصوت/الترجمة/مونو-ستيريو: تقرر (بالاتفاق) عدم إضافتها بتطبيقنا — تُضبط مرة واحدة داخل إعدادات VLC نفسه وتنطبق تلقائياً بعدها (2026-07-27)
- [ ] لم تُختبر على الجهاز الفعلي بعد: المشغّل المفضل، القوائم المخصّصة، تبديل القناة عبر M3U، بقاء رمز النسخة الاحتياطية بعد إعادة التثبيت — فقط تأكدنا أن البناء ينجح
- [ ] إذن الوصول لذاكرة الجهاز (WRITE/READ_EXTERNAL_STORAGE) يُطلب الآن عند أول فتح للتطبيق — يحتاج تأكيد إن المستخدم يوافق عليه من الجهاز الفعلي وإلا ميزة "الرمز يبقى بعد إعادة التثبيت" ما تشتغل (ترجع تعمل بـlocalStorage العادي فقط)
- [x] اشتراك World 8K فيه 177 ألف فيلم — فتح "الكل" كان يجمّد التطبيق فوراً (رسم 177 ألف بطاقة DOM دفعة واحدة)، والبحث كان يفشل بصمت (مهلة 30 ثانية غير كافية لتحميل مكتبة بهذا الحجم). أُصلح: رسم بالدفعات (200 كحد أقصى + زر تحميل المزيد من نفس القائمة المحمَّلة بالذاكرة)، ومهلة اتصال 180 ثانية لطلبات "الكل" تحديداً (2026-07-27)
- [ ] لم تُختبر فعلياً على اشتراك World 8K بعد إصلاح التجمّد والبحث — يحتاج تجربة حقيقية على الجهاز
- [ ] لو تكرر فشل تحميل مكتبة ضخمة جداً حتى مع مهلة 180 ثانية: قد نحتاج حل أعمق (مثلاً تحميل المكتبة الكاملة مرة وتخزينها محلياً بدل تحميلها من جديد كل جلسة)
- [x] طُلب: معاينة مباشرة للقناة أثناء التصفح + شاشة مقسومة لقناتين بنفس الوقت. جُرِّب حل "MX Pop-up mode" (نافذة MX العائمة) كبديل بلا مخاطرة تضمين محرك — **لم يشتغل فعلياً عند التجربة الحقيقية على الجهاز**. تقرر إغلاق الموضوع حالياً: شاشة واحدة/مشغّل خارجي فقط، بلا معاينة مباشرة وبلا شاشة مقسومة (2026-07-27)
- [ ] إن أراد أبو فيصل إعادة فتح موضوع المعاينة/الشاشة المقسومة لاحقاً: الخيار الوحيد المتبقي هو تضمين محرك فيديو داخل التطبيق (نفس مخاطرة تجمّد ExoPlayer السابقة) — يحتاج نقاش وقرار صريح قبل أي محاولة

## 💬 نقاش مفتوح (2026-09-17): بناء مشغّل خاص بنا بدل VLC/MX — لم يُتخذ قرار بعد

**الفكرة**: مشغّل مدمج داخل myTv+ نبنيه على طريقتنا، بدل تسليم التشغيل لـVLC/MX.

**لماذا يُطرح الآن**: قرار ترك المحرك المدمج (2026-07-27) كان بسبب تجمّد ExoPlayer على بوكس MTC برام 1GB وخلف WebView شفاف. الجهاز الحالي TCL 65T61D (Google TV) أقوى بكثير.

**ما يحلّه**: مشكلة المشغّلات (VLC مع الأفلام / MX مع القنوات)، ظهور رقم الفيلم بدل اسمه، حفظ مكان التوقف، تبديل القنوات بلا M3U ولا خدمة Accessibility، الترجمة ولغة الصوت من داخل التطبيق، وسجلات أخطاء حقيقية للتشخيص.

**المواصفات المطلوبة أصلاً منذ نسخة سامسونج** (`iptvsung`، 2026-07-25/26، نُفِّذت بـAVPlay ثم ضاعت بالتحول لمشغّل خارجي):
- أفلام/مسلسلات: تقديم 30ث / إرجاع 10ث، أزرار متقدمة بخطوة 5ث، تقديم/إرجاع بعدد ثوانٍ مكتوب، ذهاب لدقيقة محددة، شريط تقدم، إعادة من البداية، سرعات 1.0–3x، زوم (عادي/1.25/1.5/2x)، ترجمة من فلاش USB أو رابط
- قنوات مباشرة: إعادة بث (catch-up) 10/30/60/180 دقيقة أو رقم مكتوب + "مزامنة إلى البث المباشر"، نقطة خضراء ● للقنوات الداعمة، تبديل قناة ▲/▼ أثناء العرض، تسجيل البث على فلاش USB، زوم
- قائمة المشغّل المتقدمة بالزر الأحمر 🔴
- مرجع الكود السابق: `iptvsung/app/js/player.js`

**الخيارات التقنية المطروحة**:
- media3 (ExoPlayer) بنشاط أندرويد مستقل (ليس خلف WebView) — خفيف، لكن قد يعجز عن بعض الصيغ (MKV بصوت DTS/AC3) لاعتماده على عتاد الجهاز
- libVLC مدمجة — تشغّل كل شيء تقريباً، لكن تزيد حجم APK ~30MB+ وتطويرها أثقل

**الرأي المقترح (غير مُعتمد)**: media3 بنشاط مستقل + زر احتياطي "افتح في VLC/MX" عند الفشل، والبدء بالأفلام/المسلسلات ثم القنوات. libVLC فقط إن تكرر فشل الصيغ فعلياً على الجهاز.

**أسئلة مفتوحة**: هل يكون المشغّل جزءاً من myTv+ نفسه (المقترح) أم تطبيقاً منفصلاً (مجلد `movie_player` الفارغ)؟ هل نبدأ بالتنفيذ أم نؤجل؟

### 💬 تابع النقاش (2026-09-17): الترجمة داخل المشغّل الخاص

- **الموجود سابقاً** (`iptvsung/app/js/subtitles.js`): ملف SRT من فلاش USB (الجذر + مجلدات المستوى الأول) أو من **رابط مباشر يكتبه المستخدم** لملف srt، مع تحويل الترميز العربي windows-1256 تلقائياً. لم يكن هناك بحث تلقائي عن الترجمة.
- **الأفكار المطروحة للنقاش**:
  - بحث تلقائي من الإنترنت بمعرّف الفيلم (`tmdb_id` يأتي غالباً مع `get_vod_info` من Xtream) عبر خدمة ترجمات مثل SubDL أو OpenSubtitles (تحتاج مفتاح API، وحدود التنزيل وشروطها تُراجَع قبل الاعتماد)
  - اختيار الترجمة من قائمة نتائج بدل كتابة رابط يدوياً
  - الترجمات المدمجة داخل الملف نفسه (MKV) — تُقرأ مباشرة إن كان المحرك مدمجاً
  - أداة ضبط توقيت الترجمة (تقديم/تأخير) لأن الترجمة الجاهزة كثيراً لا تطابق نسخة الفيديو
  - الفلاش يبقى خياراً احتياطياً (على أندرويد يحتاج اختيار ملف عبر نظام الملفات بدل tizen.filesystem)

### 💬 تابع النقاش (2026-09-17): ترجمة تلقائية مثل يوتيوب

- **ملاحظة أبو فيصل**: `iptvmtc` هي النسخة الأكمل حالياً والأساس لأي عمل قادم.
- **الفكرة**: ترجمة تلقائية كيوتيوب. وهي في الحقيقة ميزتان:
  1. **ترجمة آلية لترجمة موجودة** (مثلاً SRT إنجليزي ← عربي): سهلة نسبياً. خيارات: ML Kit من جوجل على الجهاز (مجانية، بلا إنترنت، جودة متوسطة) أو نموذج لغوي مثل Claude (جودة أعلى وفهم للسياق، بتكلفة بسيطة لكل فيلم). تُترجم الملف كاملاً قبل العرض.
  2. **توليد ترجمة من الصوت نفسه** (مثل Whisper) لما لا ترجمة له إطلاقاً: أصعب بكثير.
     - على التلفاز نفسه: غير عملي (معالج ضعيف لهذا الحمل).
     - سحابياً أثناء العرض: تأخير ثوانٍ + تكلفة مستمرة.
     - مسبقاً للأفلام: معالجة الفيلم على اللابتوب (i9) قبل المشاهدة وحفظ SRT جاهز. الاستضافة المشتركة hPanel لا تكفي لتشغيل Whisper.
     - القنوات المباشرة: الأصعب على الإطلاق (لحظي) — آخر الأولويات.
- **شرط أساسي**: كل هذا يتطلب المشغّل المدمج الخاص (VLC/MX لا يسمحان بالوصول للصوت ولا عرض ترجمتنا).
- **ترتيب مقترح (غير معتمد)**: ترجمة SRT الموجود للعربية أولاً ← ثم توليد ترجمة للأفلام مسبقاً ← القنوات المباشرة أخيراً إن لزم.
- **تحديث (2026-09-17)**: أبو فيصل لا يريد استضافة hPanel لهذه الميزة — المعالجة على التلفاز TCL 65T61D نفسه.
  - ترجمة SRT للعربية على الجهاز: ممكنة (ML Kit بلا إنترنت)، أو اتصال مباشر من التلفاز بواجهة API بلا أي خادم وسيط.
  - توليد الترجمة من الصوت على التلفاز: غير مؤكد — قوة التلفاز في الفيديو تأتي من رقاقة فك ترميز مخصصة، أما الذكاء الاصطناعي فيعتمد على المعالج والرام. الفكرة المقترحة: معالجة الفيلم بالخلفية مسبقاً (ولو أبطأ من زمن الفيلم) بنموذج Whisper صغير، بدل الترجمة اللحظية.
  - **قبل أي قرار**: قراءة المواصفات الفعلية (المعالج، الرام، التخزين، إصدار أندرويد) من داخل التطبيق نفسه، ثم تجربة قياس سرعة صغيرة لـWhisper على الجهاز.

### 💬 تابع النقاش (2026-09-17): حالات الترجمة ومصدر الملف (صياغة أبو فيصل + ملاحظات)

**مصادر ملف الترجمة**: مدمجة داخل الفيلم (MKV) ← موقع ترجمات بمفتاح رسمي (SubDL/OpenSubtitles) بمعرّف TMDB من Xtream ← بحث حر بالذكاء الاصطناعي (احتياطي، مع تحقق إلزامي بتنزيل الرابط) ← يدوياً (فلاش/رابط). الترجمات المحروقة داخل الصورة لا يمكن التعامل معها.

**تقسيم العمل المقترح**: التطبيق يجلب قائمة الترجمات، والذكاء الاصطناعي يختار الأنسب لنسخة فيلمنا (اسم الإصدار، المدة، مقارنة التوقيت بترجمة إنجليزية مدمجة إن وُجدت).

**الحالات**:
1. **ترجمة عربية موجودة لكن توقيتها غير مضبوط** ← ضبط التوقيت. الذكاء لا يسمع الفيلم، فيحتاج مرجعاً:
   - مرجع ترجمة إنجليزية مضبوطة (مدمجة أو مطابقة): الذكاء يطابق كل جملة عربية بمقابلها الإنجليزي وينقل توقيتها — الأدق.
   - بلا مرجع: فرق ثابت أو فرق سرعة إطارات يُحسب رياضياً من نقطتين (بلا ذكاء) + زر تقديم/تأخير يدوي.
   - مرجع الصوت نفسه (توقيتات Whisper) — يرتبط بالحالة 2-ج.
2. **لا توجد إلا ترجمة أجنبية** (بعد فشل البحث عن عربية — البحث عن رابط عربي يسبق الترجمة لأنه أرخص وأدق):
   - أ. الذكاء يترجم الملف للعربية ويعيد ملف SRT بنفس التوقيتات الأصلية، والتطبيق يعرضه ويحفظه (لا تُكرَّر الترجمة ولا التكلفة).
   - ب. (قبل أ فعلياً) الذكاء يبحث ويعطي التطبيق رابط ترجمة عربية، والتطبيق ينزّلها ويتحقق منها.
   - ج. الترجمة الصوتية (Whisper ثم ترجمة) — للأفلام بلا أي ترجمة، وتعطي أيضاً مرجع توقيت للحالة 1. قابليتها على التلفاز غير مؤكدة.
- **التكلفة التقديرية لترجمة فيلم**: Haiku 4.5 نحو 0.2$، Sonnet 5 نحو 0.4$، Opus 5 نحو 1$ (تقدير، يُقاس فعلياً). مفتاح API داخل APK مقبول لتطبيق شخصي فقط مع حد إنفاق شهري.
- **اقتراح قياس قبل القرار**: عيّنة 20 فيلماً من "الاشتراك الأول" — كم منها بترجمة مدمجة، وكم له عربية جاهزة في SubDL.
- **توضيح أبو فيصل (2026-09-17)**: "الترجمة الصوتية" = ترجمة تلقائية كيوتيوب (من كلام الفيلم نفسه إلى نص عربي) — **آخر حل** بعد كل المصادر السابقة.
  - يوتيوب يولّدها مسبقاً على خوادمه عند رفع الفيديو، لا لحظياً أثناء المشاهدة.
  - عندنا طريقتان: (أ) لحظياً أثناء المشاهدة عبر خدمة سحابية للتعرف على الكلام (تأخير ثانية-ثانيتين، تكلفة لكل ساعة مشاهدة، تصلح للقنوات المباشرة أيضاً)، (ب) مسبقاً قبل المشاهدة على التلفاز نفسه بنموذج Whisper (بلا تكلفة، لكن سرعته على المعالج غير مؤكدة).
  - **زمن الترجمة الصوتية اللحظية (تقديرات عامة، تُقاس فعلياً)**: تحويل الكلام لنص ~0.5-1ث بعد انتهاء الجملة + ترجمة الجملة ~0.5-1.5ث = تأخر ~1-3ث خلف الكلام.
    - الحل للأفلام: المشغّل الخاص يقرأ الصوت **قبل موضع العرض** بدقيقة مثلاً (الملف متاح كاملاً)، فتجهز الترجمة قبل أن تُقال الجملة = بلا تأخر ظاهر، وانتظار ثوانٍ فقط عند البداية.
    - القنوات المباشرة: تأخير عرض الصورة 3 ثوانٍ عمداً ليتزامن النص، أو قبول تأخر النص.

### ✅ قرار (2026-09-17): المشغّل تطبيق مستقل
- مشغّل فيديو مستقل مثل VLC/MX، يُستخدم مع myTv+ أو وحده — مشروعه `movie_player`.
- قائمة المواصفات وترتيب الترجمة النهائي في `movie_player/Todo.md`.

### ✅ نُفِّذ (2026-09-17): بناء المشغّل وربطه بـmyTv+
- المشغّل بُني واختُبر على محاكي Google TV ومربوط بـmyTv+ — التفاصيل في `readme.md` هنا و`movie_player/README.md`.
- المتبقي والملاحظات (رصيد مفتاح Claude نفد أثناء الاختبار، ما لم يُختبر على الجهاز الحقيقي) في `movie_player/Todo.md`.

### 🔄 myTv+ 1.1.0 (2026-09-17 مساءً)
- [x] تثبيت Player+ من داخل myTv+، التحديث التلقائي للتطبيقين، مفتاح الترجمة قبل التشغيل، بيانات الفيلم كاملة — اختُبرت على المحاكي.
- [x] **رُفعت** `SmartTVPlus.apk` و`PlayerPlus.apk` و`versions.json` إلى الإصدار v1.0.0 (2026-09-17 مساءً) بأمر أبو فيصل، واختُبر التثبيت من الرابط الحقيقي على المحاكي بنجاح.
- [ ] التحديث الأول على التلفاز يدوي (النسخة الحالية 1.0.0 بلا نظام تحديث).

### ⬇️ myTv+ 1.2.0 (2026-09-18)
- [x] بند «تنزيل وتثبيت مشغّل Player+» في الشاشة الرئيسية + رفع النسخة على GitHub — اختُبر على المحاكي.
- [ ] على التلفاز: تحديث myTv+ يدوياً مرة واحدة (النسخة القديمة 1.0.0 بلا نظام تحديث)، ثم يظهر البند لتثبيت المشغّل.

### 🔢 1.2.1 / 1.1.1 (2026-09-18)
- [x] رقم الإصدار ظاهر داخل التطبيقين ورُفعت النسخ — للتأكد من النسخة العاملة على التلفاز.

### 🔐 التوقيع (2026-09-18)
- [x] مفتاح إصدار ثابت `~/keystores/oqod-release.jks` + توقيع التطبيقين + رفعهما (myTv+ 1.3.0، Player+ 1.2.0).
- [ ] **نسخة احتياطية لملف المفتاح** — فقدانه يمنع أي تحديث مستقبلي فوق النسخة المثبّتة.

### 🎬 حل اسم الفيلم وموفر البيانات MetaProvider (2026-09-18)
- [x] تفعيل وتسجيل MetaProvider برقم البث + حفظ بيانات الفيلم عند الفتح + اعتماد Player+ تلقائياً (myTv+ 1.4.0 و Player+ 1.3.0).
- [x] بناء التطبيقين وتوقيعهما بمفتاح الإصدار الثابت ورفعهما إلى GitHub Release v1.0.0.
